(3820)

Formation

formation déclarationd des revenus 9 avril 2026

// a faire

export excel à revoir

les fichiers doivent être en véritable format excel (ils doivent être au format officiel, traitable par excel ou number de mac os ?)

les formats des cellules qui contiennent des chiffre, doivent etre au format number sinon pas possible de calculer ? on connait les champs des colonnes des chiffres à l'avance peut être configurer comme il faut ?

tableau des commisions maximum 10 champs par tableau, et pagination pour pouvoir naviguer (regarde shadcn component)

dans le excel en tete de colonnes à garder au dessus des valeurs, trouver un moyen de garder les titres des colonnes, comment fair epour garder les entetes des colonnes sachant que des fournissuer sont 1 ligne de titre, d'autres 2, dautres plus loin dans le fichier

doit on changer de library pour pouvoir ajotuer des formats / couleurs sur des champs pour traiter les excels ? là on utilise xlsx mais ya mieux ?

possible d'avoir un zoom de base ? genre on veut que se fichier soit à 50 % du zoom de base , dis moi si oui ou non, comme ça un client peut direct copié un gros tableau

independent corriger la type dans l'export

rajotuer colonne total production + encours (total commision) apres encours

enelevr dans l'export retrocession production + retrocession encours + total retrocession

le tableau des commission se refresh tousles combien de temps ? on peut mettre un cache de 15 minutes avec react query

si une commission à 0 sur un fournisseur  partout, c'est affiché dans l'export ? C'est pas mieux d'afficher tous le temps tous les fournisseurs , comme ça même si 0 le excel à toujours la même structure, et les clients auront toujours la même tête du fichier excel

pourquoiu pas avoir un template par défaut, de tous les mois, janvier, février etc.. avec tous le stableaux préremplis à 0, et nous juste par dessus on vient mettre les bonnes valeurs là où il faut

ressortir les montants globaux en couleur (pas rouge, mais bleu), afficher des alternations de couleurs entre les lignes comme le template

enlever la TVA du tableaue export dans le résumé

rétrocession quoi faire ??
